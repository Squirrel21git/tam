<Window x:Class="kartkowkaStyleWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kartkowkaStyleWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Label" x:Key="color1">
            <Setter Property="Foreground" Value="Crimson"/>
        </Style>
        <Style TargetType="Label" x:Key="color2">
            <Setter Property="Foreground" Value="Green"/>
        </Style>
        <Style TargetType="Label" x:Key="color3">
            <Setter Property="Foreground" Value="Orange"/>
        </Style>
    </Window.Resources>
    
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBox Width="250" x:Name="userText"/>
            <Button Content="Zatwierdź" Click="Button_Click"/>
        </StackPanel>
        <StackPanel x:Name="results">
            
        </StackPanel>
    </StackPanel>
</Window>
